package net.projecteuler.problems

import net.projecteuler.api.PenceAmount

fun main(args: Array<String>) {
    println("configurations: ${configurationCount(5)}")
}

fun configurationCount(amount: Int, current: PenceAmount = PenceAmount.ZERO) {

}
