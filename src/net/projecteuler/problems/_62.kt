package net.projecteuler.problems

/**
 * TODO solve.
 */

private val cubes = LongArray(2000, { it*it*it.toLong() })

fun main(args: Array<String>) = println(cubicPermutations())

private fun cubicPermutations(): Long {
    return 0
}